import React from 'react';
import { hot } from 'react-hot-loader';
import './App.css';

const App = () => (
    <div className="App">
     test new hello 
    </div>
);

export default hot(module)(App);